
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
  <title>麦饭石不粘锅 28cm｜悦厨家居</title>
  <meta name="description" content="悦厨家居麦饭石不粘锅 28cm，无油烟不粘，导热均匀，适配燃气/电磁/电陶等多种炉灶，食品级材质认证，7天无理由退换，一年质保。" />
  <meta name="keywords" content="悦厨家居, 麦饭石, 不粘锅, 厨具, 锅具, 电磁炉适配, 食品级材质" />
  <meta name="theme-color" content="#2C7A7B">
  <style>
    /* ========== CSS 变量与基础样式 ========== */
    :root{
      --brand:#2C7A7B;            /* 品牌主色 */
      --accent:#E53E3E;           /* 强调色（价格/CTA）*/
      --text:#1A202C;             /* 主文本 */
      --muted:#4A5568;            /* 次文本 */
      --line:#E2E8F0;             /* 分割线 */
      --bg:#F7FAFC;               /* 背景 */
      --card:#FFFFFF;             /* 卡片背景 */
      --radius:10px;
      --shadow:0 8px 24px rgba(0,0,0,.06);
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; font:14px/1.6 -apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Helvetica,Arial,"PingFang SC","Hiragino Sans GB","Microsoft YaHei","Noto Sans CJK SC",sans-serif;
      color:var(--text); background:var(--bg);
    }
    a{color:var(--brand); text-decoration:none}
    img{max-width:100%; display:block}
    button{font:inherit; cursor:pointer}
    .container{max-width:1200px; margin:0 auto; padding:0 16px}
    .visually-hidden{position:absolute !important; clip:rect(1px,1px,1px,1px); padding:0;border:0;height:1px;width:1px;overflow:hidden}

    /* ========== 顶栏 ========== */
    header{
      background:#fff; position:sticky; top:0; z-index:40; border-bottom:1px solid var(--line);
      backdrop-filter:saturate(180%) blur(8px);
    }
    .topbar{display:flex; align-items:center; gap:16px; padding:12px 0}
    .logo{font-weight:800; letter-spacing:.5px; color:var(--brand); font-size:18px}
    .nav{display:flex; gap:16px; align-items:center; flex:1}
    .nav a{color:var(--muted)}
    .search{flex:1; display:flex; gap:8px; max-width:520px}
    .search input{
      flex:1; padding:10px 12px; border:1px solid var(--line); border-radius:8px
    }
    .search button{
      background:var(--brand); color:#fff; border:none; border-radius:8px; padding:10px 14px
    }
    .cart{
      display:flex; align-items:center; gap:8px; color:var(--muted)
    }
    .cart .count{
      background:var(--accent); color:#fff; font-weight:700; padding:0 6px; border-radius:999px; font-size:12px
    }

    /* ========== 面包屑 ========== */
    .breadcrumb{color:#718096; font-size:13px; padding:10px 0}
    .breadcrumb a{color:#718096}

    /* ========== 产品主区域 ========== */
    .hero{
      display:grid; grid-template-columns: 1.1fr .9fr; gap:24px; align-items:start; margin:16px 0 24px
    }
    .card{background:var(--card); border:1px solid var(--line); border-radius:var(--radius); box-shadow:var(--shadow)}
    .gallery{padding:16px}
    .gallery .main{
      position:relative; border-radius:10px; overflow:hidden; aspect-ratio:4/3; background:#f1f5f9
    }
    .gallery .main img,.gallery .main video{width:100%; height:100%; object-fit:cover}
    .gallery .thumbs{display:flex; gap:10px; margin-top:12px; overflow:auto; padding-bottom:4px}
    .thumb{flex:0 0 84px; height:84px; border-radius:8px; border:2px solid transparent; overflow:hidden; background:#f8fafc}
    .thumb[aria-current="true"]{border-color:var(--brand)}
    .thumb button{all:unset; width:100%; height:100%}
    .thumb img{width:100%; height:100%; object-fit:cover}
    .media-tools{position:absolute; inset:8px 8px auto auto; display:flex; gap:8px}
    .chip{
      background:rgba(0,0,0,.55); color:#fff; padding:6px 10px; border-radius:999px; font-size:12px
    }

    .info{padding:18px}
    .title{margin:0 0 6px; font-size:24px}
    .subtitle{margin:0 0 8px; color:var(--muted)}
    .rating{display:flex; align-items:center; gap:8px; color:#F59E0B; margin:6px 0 10px}
    .price-row{display:flex; align-items:end; gap:12px; margin:8px 0 10px}
    .price{color:var(--accent); font-weight:800; font-size:26px}
    .orig{color:#A0AEC0; text-decoration:line-through}
    .promo{display:inline-flex; gap:6px; align-items:center; background:#FFF5F5; color:#C53030; padding:4px 8px; border-radius:999px; font-size:12px}
    .selling{display:flex; flex-wrap:wrap; gap:8px; margin:10px 0 6px}
    .selling .tag{background:#EDF2F7; color:#2D3748; padding:4px 8px; border-radius:6px; font-size:12px}

    .sku-group{margin:14px 0}
    .sku-group h4{margin:8px 0; font-size:14px; color:#2D3748}
    .options{display:flex; flex-wrap:wrap; gap:8px}
    .opt{
      padding:8px 12px; border:1px solid var(--line); background:#fff; border-radius:8px; color:#2D3748
    }
    .opt[aria-pressed="true"]{border-color:var(--brand); box-shadow:0 0 0 2px rgba(44,122,123,.15)}
    .opt[aria-disabled="true"]{opacity:.5; cursor:not-allowed}

    .qty{display:flex; align-items:center; gap:8px; margin:10px 0}
    .qty button{width:32px; height:32px; border-radius:8px; border:1px solid var(--line); background:#fff}
    .qty input{width:64px; text-align:center; padding:8px; border:1px solid var(--line); border-radius:8px}

    .ctas{display:flex; gap:10px; flex-wrap:wrap; margin:12px 0}
    .btn{border:none; border-radius:10px; padding:12px 16px; font-weight:700}
    .btn-primary{background:var(--accent); color:#fff}
    .btn-secondary{background:var(--brand); color:#fff}
    .btn-ghost{background:#fff; color:var(--brand); border:1px solid var(--brand)}
    .assurance{display:flex; gap:16px; flex-wrap:wrap; color:#2D3748; margin-top:10px}
    .assurance span{display:inline-flex; gap:6px; align-items:center}

    /* 服务与保障条 */
    .service-bar{
      display:flex; gap:24px; align-items:center; padding:14px 16px; margin: 6px 0 18px;
      color:#2D3748
    }

    /* 详情/规格/评价/FAQ/推荐 */
    .sections{display:grid; grid-template-columns:1fr; gap:16px; margin-bottom:28px}
    .section{padding:18px}
    .section h3{margin:0 0 12px}
    .detail img{border-radius:10px}
    .specs table{width:100%; border-collapse:collapse}
    .specs th,.specs td{border-bottom:1px solid var(--line); text-align:left; padding:10px 8px}
    .review-tools{display:flex; gap:10px; flex-wrap:wrap; margin-bottom:10px}
    .review-card{border-top:1px solid var(--line); padding:12px 0}
    .faq-item{border-top:1px solid var(--line)}
    .faq-q{width:100%; text-align:left; padding:12px 0; background:none; border:none; font-weight:700}
    .faq-a{padding:6px 0 12px; color:#2D3748; display:none}
    .faq-item[open] .faq-a{display:block}

    .related-grid{
      display:grid; grid-template-columns: repeat(4,1fr); gap:12px
    }
    .product-card{border:1px solid var(--line); border-radius:10px; overflow:hidden; background:#fff}
    .product-card .meta{padding:10px}
    .product-card .price{font-size:16px}

    /* 底部 */
    footer{border-top:1px solid var(--line); padding:20px 0; color:#4A5568; background:#fff}

    /* 移动端优化 */
    @media (max-width: 992px){
      .hero{grid-template-columns: 1fr}
      .search{display:none}
      .related-grid{grid-template-columns: repeat(2,1fr)}
    }

    /* 粘性购买条（移动端） */
    .sticky-bar{
      position:sticky; bottom:0; z-index:30; background:rgba(255,255,255,.92); backdrop-filter:blur(8px);
      border-top:1px solid var(--line); padding:8px 12px; display:none; align-items:center; justify-content:space-between; gap:10px
    }
    .sticky-bar .price{font-size:18px}
    @media (max-width: 768px){
      .sticky-bar{display:flex}
    }

    /* 轻提示 */
    .toast{
